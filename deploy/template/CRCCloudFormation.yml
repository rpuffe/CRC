Parameters:
  env: 
    Type: String
    Description: Environment name for site.
  
Resources:
  MyBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "crc-bucket-${env}"
      Tags:
      - Key: Name
        Value: crc-bucket
      - Key: Environment
        Value: !Ref env
