Parameters:
  env: 
    Type: String
    Description: Environment name for site.
  
Resources:
  MyBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: crc-bucket
      Tags:
      - Key: Name
        Value: crc-bucket
      - Key: Environment
        Value: !Ref env
